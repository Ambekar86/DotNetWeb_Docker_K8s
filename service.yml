apiVersion: v1
kind: Service
metadata:
  name: dotnet-demo-svc
  namespace: dotnet-demo-ns
spec:
  type: LoadBalancer
  selector:
    app: dotnet-demo
  ports:
    - name: http
      port: 80
      targetPort: 8080
